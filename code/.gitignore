# perception cache (content-addressed; safe to delete)
.cache/
# python
__pycache__/
*.pyc
# local secrets
.env
# large per-run prediction CSVs (regenerated by the harness)
evaluation/predictions_sample_*.csv
# NOTE: sample_stats_*.json and test_run_stats.json are tracked (committed) so a
# clean checkout can reproduce the operational/cost section of evaluation_report.md
# without spending API tokens. Regenerate them whenever the model/prompt changes.
